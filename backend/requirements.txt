# =============================================================================
# Dezider Backend - Production Runtime Requirements (top-level only)
# =============================================================================
# Cleaned 2026-02 to fix EC2 docker build failure.
# - Removed dev-only tools (black/flake8/isort/mypy/pytest) → see requirements-dev.txt
# - Removed unused libs (pandas/matplotlib/scipy/jax/sentencepiece/stripe/s5cmd/librt)
# - Removed broken pin contourpy==1.3.3 (does not exist on PyPI)
# - Fixed google-auth dev0 → stable
# Transitive deps are resolved automatically by pip.
# =============================================================================

# -- Web framework --
fastapi==0.110.1
uvicorn[standard]==0.25.0
starlette==0.37.2
python-multipart==0.0.22
slowapi==0.1.9

# -- Data / Validation --
pydantic[email]==2.12.5

# -- Database --
motor==3.3.1
pymongo==4.5.0

# -- Config / env --
python-dotenv==1.2.1
PyYAML==6.0.3
jsonschema==4.26.0
jq==1.10.0

# -- Auth / Security --
python-jose[cryptography]==3.5.0
PyJWT==2.11.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.3
pyotp==2.9.0
cryptography==46.0.5

# -- HTTP clients --
httpx==0.28.1
requests==2.32.5
aiohttp==3.13.3

# -- Scheduling --
APScheduler==3.11.2

# -- Math (mediapipe needs numpy <2) --
numpy==1.26.4

# -- Document generation / parsing --
reportlab==4.4.10
PyPDF2==3.0.1
python-docx==1.2.0
openpyxl==3.1.5
beautifulsoup4==4.14.3
lxml==6.0.2

# -- Imaging / OCR / Face Auth --
Pillow==12.1.1
pytesseract==0.3.13
opencv-python-headless==4.11.0.86
mediapipe==0.10.18

# -- Audio --
pydub==0.25.1

# -- Payments --
razorpay==2.0.1

# -- AWS (S3, etc.) --
boto3==1.42.58

# -- LLM integration (Emergent universal key) --
emergentintegrations==0.1.0
litellm==1.80.0
tiktoken==0.11.0

# -- Google APIs (Calendar, Gemini) --
google-api-python-client==2.190.0
google-auth>=2.35.0,<3.0
google-auth-httplib2==0.3.0
google-auth-oauthlib==1.3.0
google-genai==1.65.0
google-generativeai==0.8.6

# -- Search --
duckduckgo_search==8.1.1

# -- Misc utilities --
tenacity==9.1.4
typing_extensions==4.15.0
