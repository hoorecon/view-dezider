# Mobile App Runbook

1. Install Expo CLI: `npm install -g expo-cli`
2. Install dependencies: `npm install`
3. Start app: `npm run start`
4. Use a device or emulator to open the Expo session.

Update `apiBase` in `App.js` if your backend runs on a different host.
